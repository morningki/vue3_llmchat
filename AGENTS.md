# AGENTS.md

## 项目定位

这是一个 Vue3 + Vite + Pinia + Element Plus 的 LLM 对话项目，正在从纯前端项目改造成前后端分离项目。

当前目标包括：

- Node/Express 后端接入
- 普通非流式聊天接口
- SSE 流式聊天接口
- 文件上传
- RAG
- 接口联调和 code review 工作流

## Codex 职责

Codex 当前作为主开发者，负责：

- 阅读项目结构
- 规划任务
- 编写代码
- 小范围重构
- 运行检查命令
- 根据 review 反馈修复问题
- 在阶段性任务完成后更新 progress.md 或提出更新建议

## 工作流程

1. 开始任务前先阅读 progress.md。
2. 修改代码前先给出计划。
3. 只处理用户指定任务。
4. 不主动扩大任务范围。
5. 不修改无关文件。
6. 不删除已有功能。
7. 修改完成后总结改动文件和原因。
8. 能运行检查命令时，优先运行检查命令。
9. 如果检查失败，先说明失败原因和建议修复方案。

## 禁止事项

- 不读取、记录、输出 API Key、Token、Cookie、密钥信息。
- 不修改 .env、密钥配置、构建产物、依赖锁文件，除非用户明确要求。
- 不把密钥写进文档、日志或示例文件。
- 不做大范围重构，除非用户明确要求。
- 不把普通样式优化扩大成组件重写。

## 前端开发约束

- 不破坏现有聊天流程。
- 不破坏 Markdown 渲染和代码高亮逻辑。
- 不破坏 Pinia 状态结构。
- 样式修改优先采用最小改动。
- 组件修改要注意 props、emit、ref、computed、watch、生命周期和卸载清理。

## 后端开发约束

- routes 只负责声明接口路径。
- controller 负责 HTTP 层逻辑、参数校验、stream / non-stream 分支。
- service 负责调用大模型 API。
- buildChatPayload 统一构造模型请求参数。
- SSE 必须正确设置响应头、结束标记和错误处理。